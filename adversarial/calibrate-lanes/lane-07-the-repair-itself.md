# Adversarial pass — task calibrate-lanes — lane 7: The repair itself

You are an adversarial auditor with NO context about this change and NO stake in it being correct.
Your job is to REFUTE the claim that this wave is sound. Assume guilt. Every clause below is the
audit law for your lane; a wave that cannot survive its lane's clauses is a wave that ships an
escape. Report findings, not reassurance — "looks fine" is a non-answer.

## Your lane's law (escape class 7)

When auditing a fix, the fix is the most suspect code in the diff:

- The pin that asserts a true sub-statement instead of the claim (grepping for a string that
  appears in comments as well as code proves nothing).
- The repro that was never verified byte-level (mangled fixture bytes "confirm" false theories).
- The fix that adapts a surface without authenticating against the real one.
- The test added after the fact that passes against both the fixed and the broken code. Run it
  against the pre-fix source and watch it fail, or it pins nothing.

## The change under audit

```
 ...ity-trust-boundaries-that-believe-the-client.md |  99 ++++++
 ...rization-and-tenancy-who-else-can-reach-this.md |  95 ++++++
 ...dead-wiring-computed-but-consumed-by-nothing.md |  94 ++++++
 ...ams-where-a-double-stands-in-for-the-machine.md |  95 ++++++
 ...ent-drift-docs-and-controls-diverge-silently.md |  96 ++++++
 ...silent-correctness-liveness-and-reachability.md |  95 ++++++
 .../lane-07-the-repair-itself.md                   |  97 ++++++
 .../lane-08-the-self-replacing-artifact.md         |  94 ++++++
 tasks/calibrate-lanes.json                         |  63 ++++
 tasks/harness-merge-wave1.calibration.json         | 360 +++++++++++++++++++++
 tools/adversarial-runner.mjs                       | 157 ++++++++-
 11 files changed, 1342 insertions(+), 3 deletions(-)

```

Files touched:
adversarial/harness-merge-wave1-calibration/lane-01-input-forgeability-trust-boundaries-that-believe-the-client.md
adversarial/harness-merge-wave1-calibration/lane-02-authorization-and-tenancy-who-else-can-reach-this.md
adversarial/harness-merge-wave1-calibration/lane-03-dead-wiring-computed-but-consumed-by-nothing.md
adversarial/harness-merge-wave1-calibration/lane-04-mock-vs-live-seams-where-a-double-stands-in-for-the-machine.md
adversarial/harness-merge-wave1-calibration/lane-05-law-and-enforcement-drift-docs-and-controls-diverge-silently.md
adversarial/harness-merge-wave1-calibration/lane-06-silent-correctness-liveness-and-reachability.md
adversarial/harness-merge-wave1-calibration/lane-07-the-repair-itself.md
adversarial/harness-merge-wave1-calibration/lane-08-the-self-replacing-artifact.md
tasks/calibrate-lanes.json
tasks/harness-merge-wave1.calibration.json
tools/adversarial-runner.mjs


### Standing lessons from 227 past finding(s) across 32 register(s)
Recurring escape vocabulary: battery×53, commit×53, wiring×48, doctor×47, self-test×43, fence×39, evidence×37, green×32.
All 44 accepted-risk boundaries a lane must NOT re-report as novel (they are recorded WONT-FIX):
- [HIGH] advance-adoption-base f2: the advance task's only pin was degenerate: its RED was the vacuous-range refusal and its GREEN audits the self-authorizing commit — verified in form, disclosed… — a base move has no external test to pin; the pin discriminates the fence's empty-versus-nonempty range semantics, which is the change's actu…
- [HIGH] advance-adoption-base f4: commit timestamps are neutral by policy while event timestamps are honest session time: the artifact reads as backdating unless the policy is known — deliberate commit-timestamp neutrality (the public-history leak discipline) alongside honest event timestamps is standing policy, recorded i…
- [HIGH] ci-adoption-base-fallback f7: authorization is forgeable plain JSON: hand-written records and self-approvable DECISIONS.md headings pass shape checks (documented boundary class) — the plain-JSON trust boundary is documented law (WIRING: What is deliberately NOT enforced): records and registers are reviewable, git histo…
- [HIGH] narrow-base-exemption f3: the trust anchor is client-writable state: origin tracking refs and origin/HEAD are updatable in-clone, and CI checkout sets no origin/HEAD — remote-tracking refs are plain-trust state, the documented boundary of the plain-JSON register world; the git history of .stallion-base is t…
- [HIGH] wave1-red-green-pins f13: the wave's only surviving pin is the tool's own self-test: the harness grading itself, circular for self-changes — harness self-changes can only pin their own battery; the pin did discriminate (exit 1 pre-fix, 0 post-fix) and host-repo tasks pin real suit…
- [HIGH] wave2-task-binding f14: The fence-side half of the repair is unpinned end-to-end: the scopeRefusal call inside checkRange is referenced by no test — deleting it leaves self-tests, CI s… — The enforcement now lives behind citationRefusal, a pure seam pinned in both directions by self-test cases (done-task refusal, settled-histo…
- [MEDIUM] advance-adoption-base f5: the sweep range is client-aimed: prepare defaults to HEAD~1 and nothing ties the pinned span to the task's commit footprint — range-to-commit binding is the registered task-commit binding design (issue #8); until then dispatch discipline plus the pinned-sha swept ra…
- [MEDIUM] advance-adoption-base f6: the GREEN re-run leaves no durable event: the done transition is trusted computation, not recorded evidence — the transition event plus the git history of the record is the evidence; a recorded green-run event would be more client-writable prose, not…
- [MEDIUM] corrective-refusals-and-wiring f14: resolve/wont-fix accept semantically empty evidence: any existing file resolves a CRITICAL finding; any non-empty string justifies a wont-fix — the machine verifies existence and shape; the truth of evidence is judged in review and preserved by git history — the register is accountab…
- [MEDIUM] corrective-refusals-and-wiring f15: red-check evidence is existence-only: any file satisfies the verifier; the pin itself is unproven by the machine — same boundary class as f14: RED evidence is existence-checked, and the demonstration lives in the session record and commit history. Machine…
- [MEDIUM] ecc-gate-round2 f2: The gate is wired into no transport anywhere — the only wiring is WIRING.md prose; the doctor certifies the self-test runs in the battery, never that an act-bou… — Agent-hook settings are user-environment configuration, not repo law: shipping a committed .claude/settings.json would silently change behav…
- [MEDIUM] ecc-integrity-records f4: README overclaims the chain law as universal: 'every event is hash-chained... a hand edit breaks the chain' — 9 of 10 shipped records are pre-cutover and unchai… — The backdated well-formed stamp is the pre-existing, documented plain-JSON trust boundary (every cutover law shares the predicate): the coun…
- [MEDIUM] ecc-integrity-records f7: The wave's verification arc is anchored to an untracked record: the four RED pins, the adoption, and the tamper probe are evidenced only by tasks/ecc-integrity-… — Records commit at done by this repo's norm, so mid-flight verification state is inherently session-anchored; the durable evidence is the pin…
- [MEDIUM] fence-root-blindness f1: the ordinary-merge false-positive half of this fix (merges of already-footered branches must PASS) carries no machine pin: the recorded pin spends its discrimin… — the pass-half's discriminating RED existed only before the fix, when no pin was recorded; the standing proof is now production evidence — th…
- [MEDIUM] gap-hardening f7: workspaceSiblingPath still stamps the harness brand when vendored nested, and splits POSIX-only despite the URL-decode fix — nested vendoring is an unsupported layout by WIRING section 1 (tools at the repo root); at root level the derivation names the host repo. Wi…
- [MEDIUM] gap-hardening f8: the wave's own task record, register, and bundles were untracked at audit time; commit timestamps are neutral by policy while event timestamps are honest sessio… — records land with the wave they authorize, before any push (established procedure); commit-timestamp neutrality is deliberate policy while e…
- [MEDIUM] harness-merge-wave1 f19: the stripComments repair at the doctor's battery-membership walk is pinned by nothing: reverting the call site to the unstripped read leaves the whole battery g… — Accurate and registered as future work: pinning the doctor's walk requires a fixture-tree driver (the self-test cases apply the strip at tes…
- [MEDIUM] narrow-base-exemption f4: a stale origin/HEAD anchors the verdict on the wrong branch; the CI --base event.before sha can never equal the old base VALUE, so a compliant move would die on… — stale origin/HEAD is the set-head -a discipline (future doctor check); the CI shape concern is real and registered — runner-anchored verdict…
- [MEDIUM] review-fixes f5: The once-per-session destructive key lets the client spend the single ask on the cheapest classified danger, suppressing facts for every other destructive comma… — This is the spec's stated shape (issue #14: 'first destructive command per session'), confirmed in the grilling tree; the 30-minute TTL boun…
- [MEDIUM] wave1-red-green-pins f12: the 300s timeout kills only the sh child; background grandchildren survive as orphans — pins are test commands, not daemons; process-group kill on timeout is registered future work — the boundary is documented and the no-verdict…
- [MEDIUM] wave1-red-green-pins f14: evidence paths accept absolute paths outside the repo and resolve cwd-first, so a same-named file anywhere validates — repo-confinement and cwd-ordering of evidence paths are the documented plain-trust boundary, shared with the RED-check law; tightening lands…
- [MEDIUM] wave2-task-binding f3: Owner approval is authenticated by a document the same client authors (decisionHeadingExists reads working-tree DECISIONS.md appendable in the same push) — pre-… — Pre-existing, deliberately recorded trust boundary (fence header + DECISIONS preamble): the register is plain markdown whose git history is …
- [MEDIUM] wave2-task-binding f8: The fence judges every commit against the push-TIP's record; one commit can carry both a scope-widening and the out-of-scope code with no machine-readable trail… — Per-commit record archaeology (git show <sha>:tasks/<id>.json) breaks the legal flow — records legitimately land one commit after the code t…
- [LOW] bench-kit-repairs f2: the killed-run refusal has a live probe (hanging module, ungradable JSON, exit 1) but no battery pin — runSuite's timeout is hardcoded 60s and a battery case wo… — runSuite's 60s timeout is hardcoded and a battery case would pay it on every battery run forever; the live probe (hanging module, ungradable…
- [LOW] ci-adoption-base-fallback f17: CI=true visibly skips the activation checks: a client-set env remains the discriminator (documented compromise, clone config cannot travel) — clone-local config cannot travel to CI; the doctor visibly prints the skip and the push CI re-fences — the alternative (failing every CI run…
- [LOW] ci-adoption-base-fallback f23: register correction: f21 cited a refute bundle as resolution evidence for the missing task-to-file tenancy, which is registered future work, not a repair — corr… — task-to-file binding is the registered task-commit binding future work; today the push gate demands a per-commit footer naming a specific va…
- [LOW] corrective-refusals-and-wiring f18: classifier sanity check is self-satisfying in the canonical repo (the doctor classifies its own file), incapable of failing at home — the classifier check targets adopting repos whose code lives outside the vendored tools tree; in the canonical repo it is self-satisfied by …
- [LOW] ecc-efficiency-shapes f7: The dedup refusal's remedy is unexecutable for closed findings (resolve/wont-fix refuses on non-UNRESOLVED); duplicateEvidenceOf is imported dead into task-stat… — The banner derivations landed (task-state and task-findings now derive); the dead import is dropped and the remedy reworded in the repair co…
- [LOW] ecc-gate-round2 f3: Session state files are written and never pruned (a stale session leaves gate-state-*.json forever, gitignored and invisible); the task-coverage banner still ha… — Session state files are gitignored, bounded per file, and tiny; pruning them is janitorial work the TTL already neutralizes semantically (a …
- [LOW] ecc-integrity-records f10: The proof requirement checks non-emptiness only — '--proof x' passes CLI and schema, so the concrete-scenario obligation stays prompt-enforced while advertised … — Non-emptiness is the schema's bar — the same class as every free-text justification the register already carries (wont-fix reasons, pin-exem…
- [LOW] enforcement-plugin f2: the banner's additionalContext JSON shape cannot be validated outside the client; fail-open means a runner schema drift silently no-ops the banner while the gat… — the banner is advisory context by design and fails open; its JSON shape can only be validated by the client runner that consumes it — the RE…
- [LOW] enforcement-plugin f3: the gate trusts the runner-supplied cwd as its walk-up start — a lying payload could point at a different harness repo and borrow ITS tasks — the hook payload is the runner's own trust boundary; the staged gate and push fence re-judge every edit against the repo's own ROOT at commi…
- [LOW] fence-root-blindness f2: the octopus-merge intersection path (lists.every containment across 3+ parents) is verified by review only — no live probe was built for it — the octopus intersection is three lines of containment over per-parent diffs, review-verified against the two-parent live probes on both sid…
- [LOW] fix-two-step-guard f5: register correction: f1 was resolved with evidence 'fee5130' — a commit hash typed where a path belongs; the intended evidence is tools/task-coverage.mjs at com… — the phantom evidence path in f1 cannot be edited out of an append-only register; the done gate passed before the typo and verdict-on-done is…
- [LOW] gap-hardening f11: banner counts drifted in task-findings and README said four scripts where five exist — bearer citations are the documented plain-JSON trust boundary (issue #8); task-to-file binding and content-bound evidence are registered the…
- [LOW] gap-hardening f12: git stderr is suppressed in gitOut, trading diagnosability for noise-free gates — stderr suppression in gitOut is deliberate: expected-absent probes (git show of a path missing from an older rev) printed fatal noise during…
- [LOW] gap-hardening f13: register correction: f9 (task-to-file tenancy, bearer citations) was resolved with evidence that does not prove a repair, and f11 (banner drift) received a just… — bearer citations are the documented plain-JSON trust boundary and the registered design work of issue #8; today the gates demand per-commit …
- [LOW] harness-merge-wave1 f2: remote-string-lint honors a self-authored in-file exemption marker (lint-allow-local-expansion): the policed file supplies both the value and the permission. De… — Deliberate upstream design, kept: the marker is 'marked, not guessed' — an in-file lint-allow-local-expansion marker that suppresses local-e…
- [LOW] harness-merge-wave1 f3: complexity-gate --update-baseline bulk re-stamps every ceiling at current values with no justification or hash binding — the rise-forgiveness path is unaudited,… — Kept upstream's deliberate asymmetry: --update-baseline exists because the stale-baseline law (row gone or fallen under threshold = dead gat…
- [LOW] review-action-items f2: the review's batteryGreen-defaults-true judgement call: evaluateTransition's facts.batteryGreen defaults true, so a caller who forgets to run the battery gets g… — the default is load-bearing for the advisory paths: obligations() and status deliberately call the pure judge WITHOUT fs facts — a default-f…
- [LOW] review-action-items f3: the doctor's new wiring check fails in any tree that vendors the harness without the plugin — a port-time decision the check currently makes unilaterally — the check's null-branch message already names the tension (plugin gone while its law is a battery member); whether a vendored tree ships the…
- [LOW] selftest-honesty f3: no machine guard distinguishes a VACUOUS battery member from an omitted one: the doctor proves membership, not that the member prints/verifies anything, so a fu… — membership is what a static doctor can prove; verifying a member is non-vacuous needs its OUTPUT, and counting OK lines inside the battery c…
- [LOW] selftest-honesty f4: a tools/*.mjs that only MENTIONS --self-test in a comment joins the derived list and the doctor refuses until the battery runs it — fail-closed friction by deli… — a tree file that mentions --self-test without implementing one is a lie in the repo; refusing until the battery runs it or the mention is re…
- [LOW] transport-agreement f1: the fixture ids collapse the two scoped shapes into one name (probe-<phase>-scoped), so a failure message cannot say WHICH scope shape disagreed without re-deri… — the failure text names the record id and the disagreement verdict; the scope shape is recoverable from the fixture table one line up, and th…

## The refutation contract

Refute a finding ONLY by affirmatively demonstrating from the change that it is a false positive.
If you cannot determine it, do NOT refute it — uncertainty never clears a blocker. A CRITICAL or
HIGH finding must carry proof: the exact evidence (file:line or command output) AND a concrete
failure scenario — the input or state that produces the outcome, and why the existing guards miss
it. If you cannot produce both, demote the severity or drop the finding. Returning zero findings
is valid and expected: manufactured findings are the primary failure mode of LLM reviewers. A
verifier or lane that fails to return at all leaves the finding BLOCKING — silence never clears
a blocker, and a dead verifier's findings stay exactly as they were recorded.

## Output contract (exactly one JSON object, nothing else)

{"findings": [{"severity": "CRITICAL|HIGH|MEDIUM|LOW", "claim": "<what is wrong, where, and why it escapes>", "evidence": "<file:line or command output>", "proof": "<REQUIRED for CRITICAL|HIGH: the concrete failure scenario — what input/state breaks, and why existing guards miss it>"}]}

If your lane truly finds nothing, answer {"findings": []} — but say so only after checking every
clause above against every file in the diff, not after the first file.
